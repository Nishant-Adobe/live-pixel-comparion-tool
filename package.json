{
  "name": "pixcel-tool",
  "version": "0.1.0",
  "private": true,
  "description": "Local pixel comparison tool for opening two URLs side by side and highlighting DOM mismatches.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/comparison.js && node --check public/app.js && node --check scripts/capture-storage-state.js",
    "capture:storage": "node scripts/capture-storage-state.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pixelmatch": "^6.0.0",
    "playwright": "^1.52.0",
    "pngjs": "^7.0.0"
  }
}
